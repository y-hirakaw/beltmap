# beltmap v0.1 初版計画書

AIループ/ファクトリーの構成を自動推測し、TUIで地図として可視化するツール。
本書はClaude Codeによる実装を前提とした計画書である。実装前にPhase 0の調査結果で本書を更新すること。

---

## 1. 背景と目的

ループ(定期実行されるAIエージェントの処理単位)を複数接続したファクトリーを構築・運用していると、各機械(ループ)が何をしているか、issueがどういう判定でコンベア(ラベル遷移)を流れるかが人間の脳内に収まらなくなる。巨大なソフトウェアの構成を全部記憶できないのと同じ問題である。

本ツールの目的:

1. 「ここどうなってたっけ?」に数秒で答える(現状把握)
2. 人に説明するための図を常に最新状態から生成する(説明コスト削減)
3. 未完成・未接続の部分を可視化する(建設管理)

## 2. スコープ(v0.1)

### 対象

- 実行環境: macOS
- 対象ファクトリー: Claude Code / Claude Desktop / Claude web を実行基盤とするループ・ファクトリー
- 状態ストア: GitHub Issues / PRs / ラベル(`gh` CLI経由)
- 構成情報源: 対象gitリポジトリ内のファイル(`.claude/` 配下、skills、ラッパースクリプト、workflow yml等)

### 非スコープ(v0.1ではやらない)

- Claude以外のプロバイダ(Codex等)対応
- Linux / Windows対応
- ツールからの工場の編集・操作(読み取り専用とする)
- Web UI(TUIのみ)
- 複数工場の横断(1回の起動で扱うのは1工場)
- 複数マシンのIR統合(5.3「地図はマシン依存である」参照)

注: 「1工場」は「1リポジトリ」を意味しない。**定義の在処と状態の在処は別リポジトリでありうる**(5.4参照)。v0.1は1工場を扱うが、その工場が2つのリポジトリにまたがることは想定内とする。

## 3. 設計原則

この4つは実装上の判断に迷ったときの優先基準である。

1. **人間に定義書を学ばせない。** ユーザーが手で書くのは設定ファイル(どのAIを使うか・どこから情報を取るか)のみ。工場の定義そのものはツールが生成する。
2. **実態が正、定義はキャッシュ。** スキャン結果から生成した内部表現(IR)は導出物であり、ユーザーの編集対象ではない。工場が変われば再スキャンで地図が追従する。
3. **決定論を優先し、AIは補完に使う。** ラベル集計・ファイル走査など決定的に取れる情報はAIに聞かない。AI推測は決定論で埋まらない部分にのみ使い、結果は必ず「推測」として区別する。
4. **確信度を常に表示する。** 実測(確定)・AI推測・不明を視覚的に区別する。推測を確定と誤認させる表示は幻覚コンベアの実害につながるため禁止。

## 4. アーキテクチャ概観

```
[Collectors] --決定論--> [IR (内部表現/キャッシュ)]
 (gh CLI / REST /              |    ^
  ファイル走査)                 |    | 検証して取り込み
                               v    |
                    [enrichment-request.json]  [enrichment.json]
                               |                    ^
                               |   ユーザーのClaudeセッションが
                               +--> [beltmap-enrich skill] ----+
                               |
                               v
                          [TUI Renderer] --> マップ表示 / Mermaidエクスポート
                               ^
                          [Confirmer] (y/n確認 → IRに永続化)
```

- **Collectors**: 情報源ごとの読み取り部品。決定論のみ。並列実行可。
- **IR**: 工場のグラフ表現。JSON。`.beltmap/` ディレクトリ(対象リポジトリ直下、gitignore推奨)に保存。
- **推測層**: **beltmap自身はAIを呼ばない。**決定論で埋まらない穴を`enrichment-request.json`に書き出し、ユーザーのClaudeセッションがskill経由で埋めて`enrichment.json`を返す。beltmapはそれをスキーマ検証してから取り込む(5.2)。
- **TUI Renderer**: IR+ライブ状態を描画。骨格(決定論部分)を即時表示し、推測は取り込み済みのものがあれば流し込む。
- **Confirmer**: 推測辺にカーソルを当ててy/nで確定/否定。回答はIRに`confirmed`として永続化され、以後のスキャンで再質問しない。

**beltmapのバイナリにLLM呼び出しは一切含まれない。**これはテスト容易性(テストスイートがモデルに依存しない)と、課金・認証経路をツールが持たないことの両方を意味する。

## 5. データソースと収集戦略

### 5.1 決定論コレクター(Phase 1)

| コレクター | 取得元 | 取得内容 |
|---|---|---|
| labels | `gh label list` / `gh issue list` | ラベル一覧、ラベル別issue件数、各issueの現在ラベル(=レーンと在庫) |
| claude-dir | `./.claude/` および `~/.claude/`(`CLAUDE_CONFIG_DIR`があればそちら) | settings.json(permissions, hooks)、skills/`*/SKILL.md`、commands/、agents/、CLAUDE.md の存在と一覧 |
| routines | claude.ai REST API `GET /v1/code/triggers`(Phase 0で実証済) | クラウドルーチンの定義一式。**完全な構造化JSON**。下記参照 |
| desktop-tasks | `~/Library/Application Support/Claude/{claude-code-sessions,local-agent-mode-sessions}/*/*/scheduled-tasks.json` + 各`SKILL.md` | ローカルタスクの定義一式。**レジストリは2系統ある**。下記参照 |
| scripts | 設定で指定したラッパースクリプト置き場 | スクリプト一覧(内容解釈はAI層) |
| actions | `.github/workflows/*.yml` | スケジュール/イベントトリガーの定義 |
| transitions | `gh api repos/{owner}/{repo}/issues/events`(リポジトリ単位・1系列をページング) | 全issue横断のラベル付与/剥奪イベント(`created_at` / `issue.number` / `event` / `label.name` / `actor.login`)。実測遷移=実線の根拠 |

注: `.claude/` 配下の構成(settings.json / skills / hooks等)の正確な仕様は公式ドキュメント https://code.claude.com/docs/en/claude-directory を実装時に参照すること。

#### transitionsコレクターの注意点

- **per-issue timelineは使わない。** `issues/{n}/timeline` はissue数だけAPIコールを消費する。リポジトリ単位の `issues/events` なら同じラベルイベントが1系列で取れる(Phase 0で両方実証済)。core rate limitは5000/hなので、これで実用上の制限は事実上消える。
- **`actor.login` で機械は特定できない。** クラウドルーチンの操作は実行者のGitHubアカウントとして記録される(公式仕様)。したがってactorで区別できるのは「人間/ルーチン群」対「GitHub Actions等のbot」までであり、ルーチン同士は区別できない。**どの機械がその遷移を起こしたかの帰属は決定論では埋まらず、AI推測層(5.2)の担当領域である。**これは設計原則3の適用範囲を狭める発見ではなく、逆にAI層が必要な理由を具体化するものである。

### 5.2 推測層(Phase 3): skill方式

**`claude -p`は使わない。**beltmapのプロセスからLLMを呼ぶ設計を全面的に廃し、**ユーザーが既に開いているClaudeセッションに埋めてもらう**方式を採る。

#### なぜこうするか

`claude -p`はサブスクリプション利用量を消費し、認証形態によってはAPI課金になる。ツールが黙って課金経路を持つのは、可視化ツールとして筋が悪い。一方skill方式では:

- **beltmapは課金経路も認証情報も持たない。**推論はユーザーが既に開始し、既に支払っているセッションの中で起きる。5.3で未解決だったAPI認証問題は、推測層に関しては消滅する
- **beltmapのテストスイートがモデルに依存しない。**コレクターも検証器も純粋な関数として書ける
- **ユーザーが推論を目撃する。**何をどう推測したかがセッションのログに残る。設計原則4(確信度を常に表示する)と相性が良い
- **Claude Code / Desktop のどちらでも動く。**skillはただのmarkdownであり、CLIのパスやバージョンに依存しない

#### やりとりの契約

1. beltmapがスキャン時、決定論で埋まらなかった穴を `.beltmap/enrichment-request.json` に書き出す。各項目は「質問」ではなく**根拠つきのタスク**とする: 対象機械のid、根拠となるソース本文(SKILL.md本文・ルーチンプロンプト・スクリプト内容)、埋めてほしいフィールド、既知の制約(実在するラベル一覧など)
2. ユーザーが自分のClaudeセッションで `beltmap-enrich` skillを起動する。skillは上記を読み、推測結果を `.beltmap/enrichment.json` に書く
3. beltmapは次回起動または`r`キーで `enrichment.json` を**スキーマ検証してから**取り込む。検証に落ちた項目はその項目だけ`unknown`に落とす(ファイル全体を捨てない、そして絶対に捏造しない)

推測させる内容は従来どおり: 機械の「読むラベル」「書くラベル」「遷移条件」「起動方式」「一言説明」、および観測遷移の機械への帰属(5.1)。

#### 陳腐化の扱い(重要)

推測はソース本文に紐づく。**各項目に根拠テキストのハッシュを持たせ、ソースが変わったら推測を無効化する。**そうしないと、skillを一度動かした後にSKILL.mdを書き換えたとき、古い推測が実線同然の顔で残り続ける。ハッシュ不一致の推測は`unknown`に戻し、`enrichment-request.json`に再度積む。

#### トレードオフ(明記する)

**推測はユーザー起動の非同期処理になる。**当初の「TUIが裏で勝手に埋める」体験は失われ、`enrichment.json`が無ければ地図は点線ゼロ・`?`だらけで出る。これを欠陥ではなく仕様として扱うため:

- TUIは「未取り込みの推測タスクが N 件あります。`beltmap-enrich` skillを実行してください」を**常時可視の位置に表示する**。穴があること自体を隠さない
- **決定論部分だけで地図が実用になることをPhase 1・2で担保しておく。**推測層は上乗せであって前提ではない。Phase 3が無くても工場の骨格は見える、という状態をPhase 2完了時点で作る

#### Phase 0での参考実測

`claude -p`は不採用だが、推測タスク自体の難易度と規模感の参考として記録しておく。fixtureのSKILL.mdから上記フィールドを抽出させた結果、**haiku相当のモデルで十分な精度が出て、1機械あたり約11.5秒**だった。100機械規模でも数分で終わる作業量であり、skill方式でユーザーのセッションを長時間占有する心配は薄い。

またこのとき、**「コードフェンス禁止」と明示してもモデルは ```json で包んで返した。**skill方式でも同じことが起きうる(skillが直接ファイルを書く場合も、フェンス混じりで書かれる可能性がある)。**検証器は「指示に従わない出力」を前提に書くこと。**同様に、日本語入力に対し`summary`が英語で返ったため、skill側で出力言語を固定すること。

#### どうしても対話的AI実行が要る場合

自動化の都合でどうしてもセッション外からAIを動かす必要が生じた場合は、`claude -p`ではなくherdr経由で対話セッションを立てる。ただしこれは**beltmap本体の機能にはしない**(herdrへの依存を配布物に持ち込まない)。開発時の調査手段に限る。

### 5.3 ルーチン/スケジュールタスクの到達性(Phase 0で解決済)

Claudeのスケジュール実行には3系統あり、到達性が異なる。**当初想定していた「手動登録fallback」はほぼ不要になった。**

| 系統 | 実体 | 到達手段 | 到達性 |
|---|---|---|---|
| クラウドルーチン | Anthropicクラウドで実行。claude.ai/code/routines または CLIの`/schedule`で作成 | REST `GET /v1/code/triggers` | **全項目到達可・完全な決定論** |
| Desktopローカルタスク | 自マシンで実行。Desktopの Routines → New routine → Local | `scheduled-tasks.json` + `SKILL.md` の2ファイル | **全項目到達可・完全な決定論** |
| `/loop` | セッション内スケジュール | (セッションスコープのため工場の常設機械ではない) | 対象外 |

#### routinesコレクター: REST API

CLIの`/schedule list`はこのエンドポイントを叩いているだけであり、**中間にモデルを挟む必要はない。**Phase 0で`list`の実レスポンスを取得し、以下のフィールドを確認した。

| フィールド | 内容 |
|---|---|
| `id` / `name` / `enabled` | ルーチン識別と有効状態 |
| `cron_expression` | スケジュール(例 `0 22 * * *`)。UTC |
| `next_run_at` / `last_fired_at` / `created_at` / `updated_at` | 実行時刻系 |
| `job_config.ccr.events[].data.message.content` | **プロンプト本文**(機械の仕様書。AI推測層への入力になる) |
| `job_config.ccr.session_context.model` | 使用モデル |
| `job_config.ccr.session_context.allowed_tools` | 許可ツール一覧 |
| `job_config.ccr.environment_id` | クラウド環境ID(ネットワーク到達範囲の手がかり) |
| `mcp_connections[]` | コネクタ名・URL・transport |
| `created_via` | 作成経路(`http_api`等) |
| `notifications.channel` | 通知先 |

他アクション: `get`(単体)、`create` / `update`(書き込み)、`run`(即時実行)。**v0.1は読み取り専用(スコープ外に書き込みがある。設計原則および2章の非スコープに従い`list`/`get`のみ使用すること。)**

含意が2つある。

1. **routinesコレクターは決定論コレクターである。**当初「`claude -p`経由なので準決定論」と整理していたが、実際は素のREST GETであり、モデルもトークン消費も介在しない。confidenceは正当に`confirmed`。
2. **`claude -p`はルーチン取得には一切不要。**AI呼び出しは5.2(推測層)だけに閉じる。

#### 未解決: 単体バイナリからの認証

このAPIはclaude.aiのOAuthトークンを要する。Phase 0の検証はClaude Code内蔵ツール経由で行っており、**そこではトークンがプロセス内で自動付与され外部に露出しない。**beltmapは独立バイナリなので、同じトークンをどこから得るかが未解決である。

- 想定される所在(macOS Keychain / `~/.claude/` 配下の資格情報ファイル)の実地確認は、本セッションでは権限分類器にブロックされたため未実施。回避はしていない。
- 選択肢: (a) ユーザーが発行したトークンを`config.toml`または環境変数で受け取る (b) Claude Codeの資格情報ストアを読む (c) routinesコレクターをv0.1では任意機能とし、未認証時は「ルーチン不明」と表示する。
- **(b)は他ツールの資格情報を勝手に読む設計であり、少なくとも明示的なオプトインなしに既定にしてはならない。**現時点の推奨は(a)+(c)の組み合わせ。
- なお本エンドポイントは公開ドキュメントに記載がない(公開ドキュメントは`/fire`のみ記述)。研究プレビュー中の破壊的変更を前提に、依存は1コレクターへ閉じ込めること。

#### 地図はマシン依存である(重要)

情報源はスコープが2種類に分かれ、**混在している**。

| スコープ | 情報源 | どのマシンから見えるか |
|---|---|---|
| アカウント全体 | クラウドルーチン(REST) | どのマシンからでも同じものが見える |
| そのマシンのみ | Desktopローカルタスク、`~/.claude/`、ラッパースクリプト | そのマシンでしか見えない |

したがって**工場を動かしていないマシンでbeltmapを起動すると、地図は黙って不完全になる。**クラウド側の機械だけが描かれ、ローカル機械は「存在しない」ように見える。これは設計原則4(推測を確定と誤認させる表示は禁止)に真正面から反する失敗モードである — 欠けていることすら表示されないため、確信度の分離では防げない。

#### v0.1の前提(決定済)

**v0.1が描くのは「beltmapを実行しているマシンから見える工場」である。**ローカル機械は実行マシンのものだけを対象とし、他マシンのローカル機械は最初から見ないものとして扱う。ユーザーによってはローカル機械が複数マシンに分散している構成もありうるが、それを1枚に統合するのはv0.1ではやらない(11章の未決事項)。

これは制限であって欠陥ではない。ただし**制限であることが画面から分かる必要がある。**穴が空いていること自体が見えないなら、確信度の分離(設計原則4)は機能しない。

対策(v0.1で必須とする):

- 起動時に「ローカル機械が0件」または「工場リポジトリと`cwd`が一致するローカルタスクが0件」を検出したら、**地図上に「このマシンにはローカル機械が見つかりません。別マシンで動作している可能性があります」と明示する。**沈黙してはならない。
- IRに`scanned_on`(ホスト名/マシン識別子)を記録する。別マシンで取ったIRと混同しないため、また将来のマシン横断統合の足がかりとして。
- クラウドルーチンの`job_config`から得られる対象リポジトリと、ローカルで見つかった機械群が食い違う場合は`unknowns`に積む。**「クラウド側には見えているのに、対応するローカル機械がこのマシンに無い」は、別マシンに機械がある強い証拠になる。**

v0.1が担保するのは「1マシンから見える範囲を、見えている範囲だと正直に示す」ところまでである。

#### desktop-tasksコレクター: 2ファイルの結合

公式ドキュメントは「スケジュール・フォルダ・モデル・有効状態は`SKILL.md`に無い」と述べており、それ自体は正しい。しかし**同じ情報が兄弟のJSONファイルに構造化されて存在する**ことをPhase 0で実測確認した(ローカルタスクを1件作成して検証)。したがって手動登録fallbackは不要になった。

**メタデータ**: `~/Library/Application Support/Claude/claude-code-sessions/<account_uuid>/<install_uuid>/scheduled-tasks.json`

```json
{
  "scheduledTasks": [
    { "id": "1", "cronExpression": "0 9 * * *", "enabled": true,
      "filePath": "/Users/…/.claude/scheduled-tasks/1/SKILL.md",
      "createdAt": 1784939869724, "model": "claude-haiku-4-5-20251001",
      "cwd": "/Users/…/git", "useWorktree": false }
  ],
  "recordedSkips": {}
}
```

**プロンプト**: 上記の`filePath`が指す`SKILL.md`。frontmatterに`name` / `description`、本文がプロンプト。

#### レジストリは2系統ある(実測で判明)

テスト工場にローカルタスクを3件登録して確認したところ、**同じファイル名・ほぼ同じスキーマのレジストリが実行系統ごとに別ディレクトリに存在する。**

| ルート | 系統 | 実測されたフィールド |
|---|---|---|
| `claude-code-sessions/*/*/` | Claude Code Desktop のローカルタスク | `cwd` / `model` / `useWorktree` あり |
| `local-agent-mode-sessions/*/*/` | ローカルエージェントモードのタスク | **`cwd` / `model` / `useWorktree` なし**。代わりに `userSelectedFolders` / `userSelectedFiles` / `notifySessionId` |

**片方だけ見ると機械を丸ごと取りこぼす。**両方をグロブすること。スキーマは共通部分(`id` / `cronExpression` / `enabled` / `filePath` / `createdAt`)で受け、系統依存のフィールドはすべて省略可能として扱う。

これは当初の記述の誤りを実測で正したものである。当初は`claude-code-sessions`だけを見ればよいと書いていたが、ユーザーがどの系統でタスクを作るかは事前に分からない。

**帰結: `cwd` は当てにできない。**5.3で「`cwd`は工場所属の決定論的な手がかり」と書いたが、これが成り立つのは片方の系統だけである。`cwd`が無い系統では`userSelectedFolders`で代替し、それも空なら**手がかり無しとして`unknown`に落とす**(推測しない)。

#### 登録された`SKILL.md`がラッパーであることがある(実測で判明)

登録タスクの`SKILL.md`が、機械の仕様そのものではなく**別の場所にある仕様書を指すだけのラッパー**であるケースを実測した。

```markdown
作業フォルダは /path/to/factory です。
このフォルダ内の .claude/skills/triage/SKILL.md を読み込み、その内容に従って実行してください。
```

**ラッパー本文にはラベルが1つも登場しない。**これを気づかずに推測層へ渡すと、材料が無い本文からラベルを推測させることになり、推測は必ず失敗する — しかも「推測できなかった」ではなく「もっともらしい嘘」が返る危険がある。

対策: **参照は決定論的に辿れる。**本文に絶対パスが書かれているので、推測層に渡す前に参照先を解決し、実体の`SKILL.md`を根拠テキストとする。解決できなければラッパー本文ではなく`unknown`を渡すこと(材料不足を推測で埋めさせない)。

実装上の注意:

- **パスの2つのUUIDをハードコードしない。**`<root>/*/*/scheduled-tasks.json` でグロブすること。account単位・インストール単位に分かれるため複数マッチしうる。
- `filePath`は絶対パスで直接入っているので、`~/.claude/scheduled-tasks/`を走査して名前で突き合わせる必要はない。`CLAUDE_CONFIG_DIR`使用時もこのフィールドが正となる。
- `cwd`は**その機械がどのリポジトリを担当しているかの決定論的な手がかり**である。複数工場対応(11章)の際の所属判定に使える。
- `recordedSkips`はスキップされた実行の記録。「動いているつもりで止まっている機械」の検出に使えるため、Phase 2のライブ状態オーバーレイで表示を検討する。
- クラウドルーチンとの重複は無い。**ローカルタスクはREST `list`に現れず、クラウドルーチンはこのJSONに現れない**(Phase 0で両方を同時に持った状態で確認済)。両コレクターの結果は単純に和集合でよく、名寄せ不要。ただしIR上は`runtime`で区別すること(実行条件が違う: ローカルはマシンが起きていないと動かない)。

### 5.4 定義リポジトリと状態リポジトリの解決

工場は2つの座標を持ち、**一致するとは限らない**。

- **定義リポジトリ**: ループのソース(skills、ラッパースクリプト、workflow yml)がある場所
- **状態リポジトリ**: issue/PR/ラベル、つまりコンベアと在庫がある場所

この2つが別リポジトリである構成は普通にありうる(ループの実装を1つのリポジトリで管理し、複数プロダクトのissueを捌く等)。**どちらか一方から他方を推測してはならない。**

#### 解決順序

1. **決定論で探す。**以下は根拠として使える。順に試し、見つかった時点で確定(`confirmed`)。
   - クラウドルーチンの`job_config`が持つ対象リポジトリ
   - Desktopローカルタスクの`cwd`
   - `.beltmap/config.toml`に前回解決した結果が記録されていれば、それ(後述)
   - ラッパースクリプト・skills本文・workflow ymlに現れる明示的なリポジトリ指定(`gh ... -R <owner>/<repo>`、`GH_REPO`、`--repo`等)。**これは文字列としての完全一致検索であり、AI推測ではない**
   - 定義リポジトリのgit remote(状態リポジトリが同一である構成の場合、これで一致する)
2. **見つからなければユーザーに聞く。**推測しない。TUI起動前に対話で状態リポジトリを確定させ、`.beltmap/config.toml`に記録して以後は聞かない。
3. **聞いた結果もIRに`provenance: user-input`として残す。**後から「なぜこのリポジトリを見ているのか」を追えるようにする。

#### 設計原則との関係

設計原則1(人間に定義書を学ばせない)は「工場の定義を書かせない」ことであって、「一切問わない」ことではない。**決定論で埋まらない座標を黙って推測するくらいなら、一度だけ聞いて確定させるほうが原則4(確信度を常に表示する)に忠実である。**質問は1回きりで、答えは永続化する。

複数の候補が見つかった場合(例: skills本文に複数リポジトリが登場)も、勝手に1つ選ばず候補を提示して選ばせること。

## 6. IRスキーマ(概略)

```json
{
  "version": 1,
  "scanned_at": "2026-07-25T09:00:00+09:00",
  "machines": [
    {
      "id": "triage",
      "name": "仕分けループ",
      "runtime": "claude-desktop-routine",
      "trigger": { "type": "schedule", "detail": "1-2h毎" },
      "reads": ["ai-process:ready"],
      "writes": ["ai-process:spec-draft", "ai-process:needs-human", "ai-process:blocked"],
      "status": "active",
      "confidence": "confirmed",
      "provenance": ["skills/triage/SKILL.md", "transition-log"],
      "summary": "readyのissueを最大3件、仕様有無で仕訳する"
    }
  ],
  "lanes": [
    { "label": "ai-process:needs-human", "count": 2, "oldest_days": 3 }
  ],
  "edges": [
    { "from": "triage", "to": "lane:ai-process:needs-human", "condition": "人の判断が必要", "confidence": "inferred" }
  ],
  "unknowns": [
    { "kind": "orphan-lane", "label": "ai-process:doc", "note": "消費する機械が見つからない" }
  ],
  "answers": [
    { "question_hash": "...", "answer": "yes", "answered_at": "..." }
  ]
}
```

- `confidence`: `confirmed`(実測/ユーザー確認済) | `inferred`(AI推測) | `unknown`
- `status`: `active` | `building`(定義はあるが遷移実績なし) | `planned`(参照のみ存在)
- `provenance`: 各判断の根拠となったファイル/ログ。詳細画面で表示する

スキーマはserdeで型定義し、versionフィールドでマイグレーション可能にしておく。

## 7. TUI画面仕様

- **マップビュー(メイン)**: 機械とレーンのグラフをASCII描画。実線=confirmed、点線=inferred、`?`=unknown、`🔨`=building。レーンには在庫数と最古滞留日数を表示、閾値超過(デフォルト3日)は警告色。

#### レーンの絞り込み(実測を受けて追加)

実リポジトリには工場と無関係なラベルが必ず混ざる。実測では14レーン中9本がGitHubの標準ラベル(`bug` / `wontfix` 等、在庫ゼロ)で、地図の6割が無関係だった。工場が育つほどこの比率は悪化する。

**ただし消してはならない。**消すと「工場に無い」と断定したことになり、判定を誤ったときに画面から気づけない。根拠つきで分類し、既定で畳む:

| 判定 | 根拠(どちらも決定論) |
|---|---|
| `factory` | 遷移が観測された / 機械の定義本文にラベル名が literal で出てくる |
| `no-evidence` | どちらも無い |

「証拠が無い」は「無関係」ではない。畳むのは既定の表示だけで、**件数は常に表示する**(「証拠が無いラベル9件は省略」)。

定義本文の文字列一致が効くのは、**まだ一度も動いていないレーン**を拾えるため。建設中の工場では遷移実績が無いレーンのほうが多い。

#### 行き止まりは2種類ある(実測を受けて追加)

当初は「流入があるのに流出が無い」を孤児レーンとしていたが、実測で**issueがそのレーンで作成された場合、行き止まりでも流入が記録されない**ことが判明した。かといって「在庫があって流出が無い＝行き止まり」にすると、まだ処理されていないだけのレーンまで断定することになる。

証拠の強さで分ける:

- `orphan-lane`: 流入が観測されているのに流出が無い。**行き止まりの強い証拠**
- `unobserved-lane`: 在庫はあるが流入も流出も観測されていない。**要確認**。行き止まりかもしれないし、単に動きが古いだけかもしれない

**この2つを同じ顔で並べてはならない。**確信度の分離(設計原則4)はAI推測との境界だけの話ではなく、決定論の内部にも証拠の強弱がある。
- **機械詳細**(Enter): summary、読む/書くラベル、トリガー、provenance、直近の遷移実績。
- **issue追跡**(i): issue番号を入力すると、そのissueの遷移履歴をライン上でハイライト。
- **確認モード**(点線にフォーカスしてy/n): 推測の確定/否定。否定時は簡単な訂正入力(自由文)を受け、次回AI推測のコンテキストに含める。
- **エクスポート**(m): Mermaid形式でクリップボード/ファイル出力。人への説明用。
- **再スキャン**(r): 差分スキャン実行。前回IRとの差分があれば「変更点」として一覧表示(ドリフト検出)。

## 8. 技術選定

- 言語: Rust(stable)
- TUI: ratatui + crossterm
- 非同期: tokio(AI呼び出し・コレクター並列用)
- シリアライズ: serde / serde_json
- GitHub: `gh` CLIをサブプロセス実行(v0.1ではoctocrab等の直APIは使わない。認証をghに委譲するため)
- AI: **呼ばない。**推測はskill経由でユーザーのセッションに委ねる(5.2)
- 設定: `.beltmap/config.toml`(状態リポジトリ、コレクターの有効/無効、パス、閾値、ルーチンAPIトークン)
- 配布: `cargo install`可能な単一バイナリ

## 9. フェーズ計画

### Phase 0: 調査スパイク — **完了(2026-07-25)**

3項目とも検証済。結果は5.1/5.2/5.3に反映済み。以下は結論のみ。

| 調査項目 | 結果 |
|---|---|
| ルーチン定義の可読性 | **想定を大きく上回る。両系統とも決定論で全項目取得可**。クラウドはREST `GET /v1/code/triggers`、ローカルは`scheduled-tasks.json`+`SKILL.md`。**手動登録fallbackは全面的に不要**になり、AI推測もモデル呼び出しも介在しない。残る課題は単体バイナリからのAPI認証のみ(5.3) |
| ラベル変更履歴の取得 | **可。かつ計画より安い経路あり**。リポジトリ単位`issues/events`で全issue横断のラベルイベントが1系列で取れる。per-issue timelineは不要。ただし`actor.login`では機械を特定できない(5.1注意点) |
| `claude -p`のJSON出力契約 | **可**。haikuで11.5秒・$0.0072/機械。ただしコードフェンス除去と出力言語固定が必須(5.2) |

検証環境: macOS / `gh` 2.87.2 / `claude` 2.1.219。

**この検証の有効範囲**: 上記で確定したのは*機構*(ファイルパスの形状、JSONスキーマ、APIの応答形、AI呼び出しのコスト特性)であり、これらはマシン非依存の事実である。一方**検証マシンは工場を稼働させているマシンではない**ため、*規模と現実性*(実際の機械数、運用中のラベル体系、遷移イベントの量、複数機械が絡む名寄せ)は未検証のまま残る。コレクターの正しさはfixtureで担保できるが、地図としての実用性(描画密度、レイアウト破綻、スキャン時間)はPhase 1を実工場で動かすまで判定できない。

**開発と検証の場所(決定済)**: 対象となる工場リポジトリはこのマシンから到達できない。`ai-process:*`系ラベルを持つリポジトリはローカル・GitHubいずれにも無く、このマシンで観測できたのはクラウドルーチン1件(プロンプト`ok`、対象リポジトリ未設定)とローカルタスク1件(検証用に作成)のみ。

**開発はこのマシンで行い、受け入れ判定は工場マシンで行う。**加えて、判定ゲートまで待たず**ある程度動く段階で早期に工場マシンでも走らせる**方針とする。これはPhase 1に運搬可能性の要件を持ち込む:

- **早期から工場マシンへ持ち込める形を維持すること。**リポジトリをgit管理下に置き、`git clone` + `cargo run` だけで別マシンで起動できる状態を常に保つ。このマシン固有のパス・設定・状態に依存しない。
- **部分データで破綻しないこと。**工場マシンでは機械数もラベル数も遷移量もこのマシンとは桁が違う。逆にこのマシンでは常に「ほぼ空の工場」しか描けない。**両極端のどちらでも破綻しない描画**をPhase 1の暗黙の要件とする(空でも「何も無い」と正しく言い、密でもレイアウトが壊れない)。
- 早期実行で得たいのは受け入れ基準の合否ではなく、**fixtureでは絶対に出ない情報**(実際の描画密度、レイアウト破綻、スキャン所要時間、想定外のラベル体系)である。走らせたら所見を本書10章のリスク欄に還流させること。

「地図はマシン依存である」(5.3)も併せて参照。

### Phase 1: 決定論スキャン+静的マップ(コア)

- config読み込み、labels/claude-dir/actions/transitionsコレクター、IR生成・保存、マップビュー描画、機械詳細
- AI呼び出しなし。この時点では点線は出ず、実線と`?`のみ
- **受け入れ基準**: 実リポジトリ(自分の工場)で起動し、レーン一覧+在庫数+観測済み遷移が地図として表示される。孤児レーン(消費者なしラベル)が`unknowns`に出る

### Phase 2: ライブ状態オーバーレイ

- レーン在庫の最古滞留日数、警告表示、issue追跡ビュー、再スキャン(r)
- **受け入れ基準**: 「needs-humanに何件・最古何日」が起動から5秒以内に見える

### Phase 3: 推測層(skill方式)

- `enrichment-request.json`の書き出し、`beltmap-enrich` skillの同梱と導入手段、`enrichment.json`の検証・取り込み、根拠ハッシュによる陳腐化判定、点線描画、確認モード(y/n永続化)、否定時の訂正入力
- **beltmapプロセスからのLLM呼び出しは実装しない**(5.2)
- **受け入れ基準**: SKILL.mdしか根拠のない機械について、skillを実行すると読む/書くラベルが点線で表示され、yで実線化し、再スキャン後も再質問されない。**かつ、根拠のSKILL.mdを書き換えると当該推測が`unknown`に戻り再度リクエストに積まれる。**skill未実行の状態でも地図が破綻せず、未取り込み件数が可視である

### Phase 4: 説明・ドリフト機能

- Mermaidエクスポート、差分スキャンの変更点一覧、`building`/`planned`ステータス判定
- **受け入れ基準**: mキーで得たMermaidがそのままZenn/GitHubに貼って描画される。工場に機械を1つ追加して再スキャンすると変更点として検出される

## 10. リスクと対策

- **~~Desktopルーチンが読めない~~** → Phase 0で解消(5.3)。両系統とも決定論で全項目読める。残存リスクは下3項に置き換わった
- **routines APIが未公開仕様である** → `GET /v1/code/triggers`は公開ドキュメントに記載がなく、ルーチン機能自体が研究プレビュー。スキーマ変更・エンドポイント消滅を前提とし、依存を1コレクターに閉じ込める。失敗時はルーチン群を`unknown`として描画し、クラッシュも捏造もしない
- **単体バイナリからのAPI認証が未解決** → 5.3参照。v0.1はトークンを設定で受け取る方式を推奨し、未認証時はクラウドルーチンを「取得不可」と明示表示して他機能は動かす。**他ツールの資格情報ストアを黙って読む実装にはしない**
- **Desktopのローカルファイル配置が変わる** → `scheduled-tasks.json`のパスもJSON構造も非公開仕様。パスはグロブで解決し、スキーマ不一致時は該当機械のみ`unknown`に落とす(全体を落とさない)
- **遷移の機械への帰属が決定論で埋まらない** → `actor.login`はクラウドルーチン同士を区別しない(5.1)。帰属はAI推測扱いとし、点線+provenance表示。ここを実線で描いてはならない
- **AI推測の幻覚** → 確信度分離+出力スキーマ検証+provenance表示。推測は決定論と混ぜて表示しない。加えて検証器はフェンス混入等の「指示に従わない出力」を前提に書く(5.2)
- **推測が陳腐化して実線同然に残る** → 根拠テキストのハッシュを推測に紐づけ、ソース変更時に自動で`unknown`へ戻す(5.2)。skill方式は推測がユーザー起動のため、放置すると古い推測が居座りやすい。ここは設計原則2(実態が正)の生命線
- **skillが実行されず点線が永久に出ない** → 決定論部分だけで地図が実用になることをPhase 2までに担保する。未取り込み件数をTUIに常時表示し、穴があること自体を隠さない(5.2)
- **状態リポジトリを取り違える** → 決定論で探して見つからなければ推測せずユーザーに聞く(5.4)。候補が複数ある場合も勝手に選ばない
- **初回スキャンが遅い** → 骨格即時表示+AI層の非同期後追い。2回目以降はgit差分で対象を絞る
- **~~timeline APIのレート制限~~** → リポジトリ単位`issues/events`への変更で実質解消(5.1)。core 5000/hに対し1リポジトリ数コール。結果は従来通りIRにキャッシュ
- **`gh`/`claude` CLI未インストール環境** → 起動時チェックで検出し、インストール手順を表示して該当機能を無効化(クラッシュさせない)

## 11. 未決事項

- ラッパースクリプト置き場のパス規約(設定で指定 or 慣習パスの自動探索)
- 複数工場対応をいつ入れるか(v0.2以降)。1工場が2リポジトリにまたがる件は5.4でv0.1のスコープ内として解決済み
- **複数マシンのIR統合**(5.3)。ローカル機械はマシンごとに散らばるため、全体像を1枚にするには各マシンのIRをマージする必要がある。v0.2以降
- **routines APIの認証方式**(5.3)。単体バイナリがclaude.aiのOAuthトークンをどう得るか。現推奨は設定で受け取る方式
- **`beltmap-enrich` skillの配布方法**(5.2)。バイナリに埋め込んで`beltmap skill install`で書き出すか、リポジトリに同梱してユーザーが`.claude/skills/`へ置くか
- 確認モードの質問文をテンプレート固定にする(v0.1はテンプレート固定。生成にAIを使わない方針が5.2で確定したため、実質決定済み)

## 12. 実装の進め方(Claude Codeへの指示)

- Phase順に実装し、各Phaseの受け入れ基準を満たしたことを確認してから次へ進む
- Phase 0の調査結果と食い違う記述が本書にあれば、実装を進める前に本書を修正して差分を報告する
- **IRスキーマ・config.tomlのキー名は、実装しながら確定させる方針とする(決定済)。**v0.1リリースまでは実データを見ながら変更してよく、変更のたびに提案する必要はない。Phase 0で`recordedSkips`や`cwd`のように事前には思いつかなかった有用フィールドが出てきたのと同じことが、コレクター実装中にも起きるため。ただし**変更したらその時点で本書の6章を更新する**こと(コードとスキーマ記述の乖離を残さない)
- v0.1リリース以降は上記を締め、外部に見える契約の変更は`version`フィールドを上げてマイグレーションを用意する
- 各コレクターは独立してテスト可能にする(fixtureのリポジトリ構造を`tests/fixtures/`に置く)